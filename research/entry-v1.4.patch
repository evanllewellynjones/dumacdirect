--- /mnt/user-data/uploads/entry.html	2026-09-08 01:30:18.245205000 +0000
+++ entry.html	2026-09-08 02:32:39.070873244 +0000
@@ -27,6 +27,7 @@
   <div class="row">
     <button class="btn" id="m_company" style="flex:1">Company Record</button>
     <button class="btn" id="m_theme" style="flex:1">Theme Record</button>
+    <button class="btn" id="m_note" style="flex:1">Quick Note</button>
   </div>
   <div class="sub" id="modehint"></div>
 </div>
@@ -86,6 +87,20 @@
   </div>
 </div>
 
+<!-- NOTE block -->
+<div class="card hide" id="blk_note">
+  <div class="frow">
+    <label for="f_topic" class="narrow">Topic</label>
+    <div class="f"><input type="text" id="f_topic" placeholder="Brazil" autocomplete="off"></div>
+  </div>
+  <div class="frow">
+    <label class="narrow" style="visibility:hidden">.</label>
+    <div class="f sub" style="margin-top:0">Optional &mdash; defaults to the subject.
+      A quick note is not a company or a theme, so nothing is required beyond
+      the subject and the note. Tags do the retrieval.</div>
+  </div>
+</div>
+
 <div class="card">
   <div class="frow">
     <label for="f_subject" class="narrow">Subject</label>
@@ -93,6 +108,37 @@
   </div>
 </div>
 
+<!-- decision block -->
+<div class="card">
+  <div class="frow">
+    <label for="f_strategy" class="narrow">Strategy</label>
+    <div class="f md"><select id="f_strategy"></select></div>
+    <div class="f tiny" id="strathint"></div>
+  </div>
+  <div class="frow">
+    <label for="f_action" class="narrow">Action</label>
+    <div class="f md"><select id="f_action"></select></div>
+    <div class="f tiny" id="actionhint"></div>
+  </div>
+  <div class="frow">
+    <label for="f_why" class="narrow">Why</label>
+    <div class="f"><input type="text" id="f_why"
+        placeholder="One line &mdash; the reason, not the summary" autocomplete="off"></div>
+    <span class="tiny" id="whycount" style="flex:0 0 auto"></span>
+  </div>
+  <div class="frow hide" id="row_source">
+    <label for="f_source" class="narrow">Source</label>
+    <div class="f"><input type="text" id="f_source"
+        placeholder="What you were reading" autocomplete="off"></div>
+  </div>
+  <div class="frow hide" id="row_outcome">
+    <label for="f_outcome" class="narrow">Check on</label>
+    <div class="f sm"><input type="date" id="f_outcome"></div>
+    <div class="f tiny">When to ask whether this call was right. Not the Review
+      Queue &mdash; that is the Review date below.</div>
+  </div>
+</div>
+
 <div class="card">
   <label for="f_note">Note</label>
   <textarea id="f_note" placeholder="Note"></textarea>
@@ -140,6 +186,20 @@
   <div id="atts"></div>
 </div>
 
+<details class="card">
+  <summary>Paste a note written elsewhere</summary>
+  <div class="sub" style="margin-top:6px">For a record drafted in Claude, or an
+    email header block. Paste the whole thing &mdash; frontmatter and body &mdash;
+    and it loads into the form above for you to check before saving. Nothing is
+    written until you press Save.</div>
+  <textarea id="pastebox" style="min-height:100px;margin-top:6px"
+    placeholder="---&#10;record_type: Note&#10;action: observation&#10;why: ...&#10;---&#10;&#10;Body"></textarea>
+  <div style="margin-top:6px;display:flex;gap:7px;align-items:center">
+    <button class="btn" id="btn_paste">Load into form</button>
+    <span class="tiny" id="pastemsg"></span>
+  </div>
+</details>
+
 </div>
 
 <div class="bar">
@@ -162,20 +222,63 @@
 }
 
 /* ---------- mode ---------- */
+const MODES = ["Company","Theme","Note"];
+const MODE_HINT = {
+  Company:"A note about one company. Ticker required. Price targets and conviction attach to you.",
+  Theme:"A note about a theme or a catalyst. Lists the tickers the theme touches.",
+  Note:"A thought that is neither a company nor a theme \u2014 Brazil, a conference, "
+      +"something you read. Tags do the retrieval."
+};
 function setMode(m){
+  if(MODES.indexOf(m)<0) m="Company";
   S.mode=m;
   $("#m_company").className = "btn"+(m==="Company"?" pri":"");
   $("#m_theme").className   = "btn"+(m==="Theme"?" pri":"");
+  $("#m_note").className    = "btn"+(m==="Note"?" pri":"");
   $("#blk_company").classList.toggle("hide", m!=="Company");
   $("#blk_theme").classList.toggle("hide",   m!=="Theme");
+  $("#blk_note").classList.toggle("hide",    m!=="Note");
   $("#blk_pt").classList.toggle("hide",      m!=="Company");
-  $("#modehint").textContent = m==="Company"
-    ? "A note about one company. Ticker required. Price targets and conviction attach to you."
-    : "A note about a theme or a catalyst. Lists the tickers the theme touches.";
+  $("#modehint").textContent = MODE_HINT[m];
   localStorage.setItem("rc_mode",m);
   renderTags();
 }
 
+/* ---------- action / why ---------- */
+/* The decision block drives two conditional rows: source is only meaningful on
+   an observation, and outcome_check_date only on the actions that make a call
+   worth scoring later. */
+const ACTION_HINT = {
+  initiate:"Opened a position.", add:"Added to a position.",
+  trim:"Trimmed a position.",   exit:"Exited a position.",
+  pass:"Looked and declined \u2014 the record nobody keeps.",
+  observation:"A reaction with no decision attached.",
+  reference:"An artifact filed for retrieval, not a view. Why is optional."
+};
+function actionsList(){ return (RC.cfg&&RC.cfg.actions)||ACTIONS; }
+function buildActions(){
+  const sel=$("#f_action"); const cur=sel.value;
+  sel.innerHTML=""; sel.appendChild(new Option("\u2014",""));
+  actionsList().forEach(a=>sel.appendChild(new Option(a,a)));
+  if(cur) sel.value=cur;
+}
+function onAction(){
+  const a=$("#f_action").value;
+  $("#actionhint").textContent = ACTION_HINT[a]||"";
+  $("#strathint").textContent = POSITION_ACTIONS.indexOf(a)>=0 && !$("#f_strategy").value
+    ? "Set this \u2014 a position change belongs to a strategy." : "";
+  $("#row_source").classList.toggle("hide", a!=="observation");
+  $("#row_outcome").classList.toggle("hide", CHECKABLE_ACTIONS.indexOf(a)<0);
+  onWhy();
+}
+function onWhy(){
+  const n=$("#f_why").value.trim().length;
+  const exempt = WHY_EXEMPT_ACTIONS.indexOf($("#f_action").value)>=0;
+  $("#whycount").textContent =
+    n>WHY_SOFT_CAP ? n+" \u2014 long for a one-liner"
+    : (!n && exempt ? "optional here" : "");
+}
+
 /* ---------- tags ---------- */
 function allTags(){
   const auto=matchTags($("#f_subject").value, $("#f_note").value)
@@ -298,63 +401,86 @@
 }
 
 /* ---------- save ---------- */
-async function save(){
-  if(!RC.dir){ flash("Choose a folder first.","err"); return; }
-  const subject=$("#f_subject").value.trim();
-  if(!subject){ flash("Subject is required.","err"); $("#f_subject").focus(); return; }
-
+/* Assembles the record, hands it to validateRecord() in rc-core, and only then
+   writes. Nothing here does its own field checking — the /note skill and the
+   email reader call the same validator against the same record shape, so a
+   rule added there applies everywhere without being duplicated here. */
+function buildRecord(){
   const rec={};
   const contributor=$("#f_contrib").value;
   const date=$("#f_date").value || new Date().toISOString().slice(0,10);
+  const subject=$("#f_subject").value.trim();
 
   if(S.mode==="Company"){
-    const entity=$("#f_entity").value.trim();
-    const ticker=$("#f_ticker").value.trim().toUpperCase();
-    if(!entity){ flash("Company name is required.","err"); $("#f_entity").focus(); return; }
-    if(!ticker){ flash("Ticker is required on a company record. "
-        + "For a private company use a P. prefix \u2014 P.SECURITIZE.","err");
-        $("#f_ticker").focus(); return; }
-    if(!TKR_RE.test(ticker)){
-      flash("Ticker must be 1-12 chars: letters, digits, dot or hyphen.","err");
-      $("#f_ticker").focus(); return; }
-    rec.entity=entity; rec.ticker=ticker; rec.tickers=[];
-    rec.listed = $("#f_listed").checked;
-
+    rec.entity=$("#f_entity").value.trim();
+    rec.ticker=$("#f_ticker").value.trim().toUpperCase()||null;
+    rec.tickers=[];
+    rec.listed=$("#f_listed").checked;
     for(const [id,sel] of [["price_target_buy","#f_ptb"],["price_target_sell","#f_pts"]]){
       const raw=$(sel).value.trim();
-      if(raw===""){ rec[id]=null; continue; }
-      if(!/^-?\d+$/.test(raw)){ flash("Price targets must be whole numbers.","err"); return; }
-      rec[id]=parseInt(raw,10);
+      rec[id] = raw==="" ? null : raw;   // validateRecord enforces whole numbers
     }
-  } else {
-    const theme=$("#f_theme").value;
-    if(!theme){ flash("Pick a theme.","err"); return; }
-    rec.entity=theme; rec.ticker=null; rec.listed=null;
+  } else if(S.mode==="Theme"){
+    rec.entity=$("#f_theme").value;
+    rec.ticker=null; rec.listed=null;
     rec.tickers=S.tickers.slice();
     rec.price_target_buy=null; rec.price_target_sell=null;
+  } else {
+    /* Quick note. entity defaults to the subject so it is never blank on disk,
+       but the user is not asked for it. */
+    rec.entity=$("#f_topic").value.trim() || subject;
+    rec.ticker=null; rec.listed=null; rec.tickers=[];
+    rec.price_target_buy=null; rec.price_target_sell=null;
   }
 
-  const review=$("#f_review").value || null;
-  const tags=currentTags().map(t=>t.tag);
-  const now=new Date().toISOString();
-  const [yy,mm]=date.split("-");
+  const review=$("#f_review").value||null;
+  Object.assign(rec,{
+    date:date, contributor:contributor, origin:"app",
+    record_type:S.mode, subject:subject,
+    strategy:$("#f_strategy").value||null,
+    action:$("#f_action").value||null,
+    why:$("#f_why").value.trim()||null,
+    source:$("#f_action").value==="observation"
+           ? ($("#f_source").value.trim()||null) : null,
+    outcome_check_date:CHECKABLE_ACTIONS.indexOf($("#f_action").value)>=0
+           ? ($("#f_outcome").value||null) : null,
+    tags:currentTags().map(t=>t.tag),
+    attachments:S.atts.map(a=>a.name),
+    conviction:S.mode==="Company" ? ($("#f_conv").value||null) : null,
+    review_date:review, review_status:review?"Pending":null,
+    priority:null, revision:1
+  });
+  return rec;
+}
 
-  let folder=date+"_"+initials(contributor)+"_"+slug(subject);
+async function save(){
+  if(!RC.dir){ flash("Choose a folder first.","err"); return; }
+
+  const rec=buildRecord();
+  const v=validateRecord(rec, RC.cfg);
+  if(!v.ok){
+    flash(v.errors[0], "err");
+    banner(v.errors.length>1 ? v.errors.join("  \u00b7  ") : "");
+    focusFor(v.errors[0]);
+    return;
+  }
+  banner(v.warnings.length ? v.warnings.join("  \u00b7  ") : "", true);
+
+  /* Coerce the validated strings to their stored types. Safe only after
+     validateRecord has passed. */
+  for(const k of ["price_target_buy","price_target_sell"])
+    rec[k] = (rec[k]===null||rec[k]==="") ? null : parseInt(rec[k],10);
+
+  const now=new Date().toISOString();
+  const [yy,mm]=rec.date.split("-");
+  let folder=rec.date+"_"+initials(rec.contributor)+"_"+slug(rec.subject);
   const root=await notesRoot(true);
   const parent=await subdir(root,[yy,mm]);
   if(await dirExists(parent,folder)){
     let i=2; while(await dirExists(parent,folder+"-"+i)) i++;
     folder=folder+"-"+i;
   }
-
-  Object.assign(rec,{
-    note_id:folder, date:date, created:now, last_updated:now,
-    contributor:contributor, record_type:S.mode, subject:subject,
-    tags:tags, attachments:S.atts.map(a=>a.name),
-    conviction:$("#f_conv").value||null,
-    review_date:review, review_status:review?"Pending":null,
-    priority:null, revision:1
-  });
+  rec.note_id=folder; rec.created=now; rec.last_updated=now;
 
   try{
     const dir=await parent.getDirectoryHandle(folder,{create:true});
@@ -362,20 +488,112 @@
     for(const a of S.atts) await writeFile(dir,a.name,a.file);
     if(S.mode==="Company") rememberPair(rec.entity,rec.ticker);
     RC.notes=null;
-    flash("Saved "+yy+"/"+mm+"/"+folder+" \u2014 "+tags.length+" tags, "
+    flash("Saved "+yy+"/"+mm+"/"+folder+" \u2014 "+rec.tags.length+" tags, "
         + S.atts.length+" attachment(s).","ok");
     clearForm(true);
   }catch(e){ flash("Save failed: "+e.message,"err"); }
 }
 
+/* Put the cursor where the first error is, so a rejected save is one keystroke
+   from being fixed rather than a hunt. */
+function focusFor(err){
+  const map=[["Subject","#f_subject"],["Ticker","#f_ticker"],
+             ["Action","#f_action"],["why","#f_why"],
+             ["Entity","#f_entity"],["Price targets","#f_ptb"],
+             ["Conviction","#f_conv"],["outcome_check_date","#f_outcome"],
+             ["review_date","#f_review"]];
+  for(const [k,sel] of map)
+    if(err.indexOf(k)>=0){ const e=$(sel); if(e && !e.closest(".hide")) e.focus(); return; }
+}
+
+/* ---------- paste a note written elsewhere ---------- */
+/* Accepts either a full frontmatter block (a record drafted in Claude) or an
+   email-style KEY: value header. Loads it into the form. Deliberately does not
+   save — the point is that a human sees the tags and the action before a file
+   exists. Attachments cannot come through this path; they need the drop zone. */
+function loadPasted(){
+  const raw=$("#pastebox").value;
+  if(!raw.trim()){ $("#pastemsg").textContent="Nothing to load."; return; }
+
+  let f={}, body="", how="";
+  const fm=parseFM(raw);
+  if(fm){ f=fm; body=fm._body||""; how="frontmatter"; }
+  else {
+    const e=parseEmailHeaders(raw);
+    f=e.fields; body=e.body; how="header block";
+    if(e.unknown.length) how += " \u2014 ignored: "+e.unknown.join(", ");
+  }
+
+  const type = MODES.indexOf(f.record_type)>=0 ? f.record_type : "Note";
+  setMode(type);
+
+  if(f.date && /^\d{4}-\d{2}-\d{2}$/.test(f.date)) $("#f_date").value=f.date;
+  if(f.contributor && [...$("#f_contrib").options].some(o=>o.value===f.contributor))
+    $("#f_contrib").value=f.contributor;
+  $("#f_subject").value = f.subject || "";
+  $("#f_note").value = body;
+
+  if(type==="Company"){
+    $("#f_entity").value = f.entity || "";
+    $("#f_ticker").value = (f.ticker||"").toUpperCase();
+    if(f.listed!==undefined) $("#f_listed").checked = !(f.listed===false||f.listed==="false");
+    $("#f_ptb").value = f.price_target_buy ?? "";
+    $("#f_pts").value = f.price_target_sell ?? "";
+    $("#f_conv").value = f.conviction || "";
+  } else if(type==="Theme"){
+    if(f.entity && [...$("#f_theme").options].some(o=>o.value===f.entity))
+      $("#f_theme").value=f.entity;
+    S.tickers = Array.isArray(f.tickers) ? f.tickers.slice() : [];
+    renderTkr();
+  } else {
+    $("#f_topic").value = f.entity || "";
+  }
+
+  if(f.strategy && [...$("#f_strategy").options].some(o=>o.value===f.strategy))
+    $("#f_strategy").value=f.strategy;
+  $("#f_action").value = actionsList().indexOf(f.action)>=0 ? f.action : "";
+  $("#f_why").value = f.why || "";
+  $("#f_source").value = f.source || "";
+  $("#f_outcome").value = f.outcome_check_date || "";
+  $("#f_review").value = f.review_date || "";
+  onAction();
+
+  /* Tags supplied in the paste are resolved against the taxonomy — invented
+     names are dropped, never created. Anything not supplied falls back to the
+     matcher running over subject and body, as a normal entry would. */
+  S.manual.clear(); S.removed.clear();
+  let tagMsg="";
+  const supplied = Array.isArray(f.tags) ? f.tags
+                 : (typeof f.tags==="string" && f.tags ? f.tags.split(",") : null);
+  if(supplied){
+    const r=resolveTagNames(supplied, RC.cfg);
+    r.tags.forEach(t=>S.manual.add(t));
+    if(r.missing.length) tagMsg=" \u2014 not in the taxonomy, dropped: "+r.missing.join(", ");
+  }
+  renderTags(); renderPicker();
+
+  $("#pastemsg").textContent="Loaded from "+how+". Check it, then Save."+tagMsg;
+  flash("Loaded into the form. Nothing saved yet.","ok");
+  $("#f_subject").focus();
+}
+
 function clearForm(keepMsg){
   $("#f_subject").value=""; $("#f_entity").value=""; $("#f_ticker").value="";
   $("#f_note").value=""; $("#f_ptb").value=""; $("#f_pts").value="";
   $("#f_conv").value=""; $("#f_review").value=""; $("#f_listed").checked=true;
+  $("#f_topic").value="";
+  $("#f_action").value=""; $("#f_why").value=""; $("#f_source").value="";
+  /* strategy is deliberately NOT cleared — someone entering five records is
+     almost always in one strategy, and re-picking it each time is friction
+     that gets skipped. */
+  $("#f_outcome").value="";
+  $("#pastebox").value=""; $("#pastemsg").textContent="";
   S.atts=[]; S.manual.clear(); S.removed.clear(); S.tickers=[]; S.imgN=0;
-  renderAtts(); renderTags(); renderPicker(); renderTkr();
+  renderAtts(); renderTags(); renderPicker(); renderTkr(); onAction();
   $("#f_date").value=new Date().toISOString().slice(0,10);
-  if(!keepMsg) flash("");
+  /* keepMsg is set after a successful save, and a save can succeed WITH
+     warnings. Clearing the banner here would wipe them before they were read. */
+  if(!keepMsg){ banner(""); flash(""); }
 }
 
 /* ---------- boot ---------- */
@@ -393,9 +611,19 @@
   th.appendChild(new Option("\u2014",""));
   (cfg.themes||[]).forEach(t=>th.appendChild(new Option(t,t)));
 
+  /* Strategies are a closed, Admin-edited list — same shape as themes. Free
+     text would give us "Direct US", "DirectUS" and "Direct U.S." inside a
+     month and the grouping query would be worthless. */
+  const st=$("#f_strategy"); st.innerHTML="";
+  st.appendChild(new Option("\u2014",""));
+  (cfg.strategies||[]).forEach(x=>st.appendChild(new Option(x,x)));
+  st.value=localStorage.getItem("rc_strategy")||"";
+
   $("#f_date").value=new Date().toISOString().slice(0,10);
   refreshLists();
+  buildActions();
   setMode(localStorage.getItem("rc_mode")||"Company");
+  onAction();
   renderTags(); renderPicker(); renderTkr();
 
   if(!RC.dir) banner("No folder connected. Click the badge, top right, and pick the "
@@ -407,6 +635,12 @@
 document.addEventListener("rc-folder", async()=>{ await boot(); });
 $("#m_company").onclick=()=>setMode("Company");
 $("#m_theme").onclick=()=>setMode("Theme");
+$("#m_note").onclick=()=>setMode("Note");
+$("#f_action").addEventListener("change",onAction);
+$("#f_strategy").addEventListener("change",()=>{
+  localStorage.setItem("rc_strategy",$("#f_strategy").value); onAction(); });
+$("#f_why").addEventListener("input",onWhy);
+$("#btn_paste").onclick=loadPasted;
 $("#f_subject").addEventListener("input",()=>{renderTags();renderPicker();});
 $("#f_note").addEventListener("input",()=>{renderTags();renderPicker();});
 $("#tagsearch").addEventListener("input",renderPicker);
